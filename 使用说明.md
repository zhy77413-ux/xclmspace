# RoboCar `fangan_core` 使用说明

## 1. 文档适用范围

本文档依据 `fangan_core` 当前源码编写，适用于已经安装 ROS 2 Humble/TROS Humble、地平线工具链以及本车硬件驱动的 RoboCar 小车环境。

本文档的目标是说明：

- `fangan_core/src` 中各源码文件的作用；
- 完整提交材料应包含的目录、模型、脚本和依赖；
- 如何在小车上检查环境、编译工程、启动节点和开始比赛；
- 如何查看运行状态、修改参数和安全停车；
- 如何排查常见的编译及运行问题。

> 重要：当前 `RoboCar_vendor_backup_20260708_002605` 是 `vendor` 源码备份，不是完整的 ROS 2 工作区。它不能单独代表可运行提交材料。正式提交时应使用完整工作区结构，并补齐本文第 5 节列出的运行依赖。

## 2. 软件功能概述

`fangan_core` 是 RoboCar 比赛流程的核心控制包。主程序 `race_main` 使用 ROS 2 多线程执行器同时运行三个节点：

| 节点 | 源码 | 主要职责 |
| --- | --- | --- |
| `mission_manager` | `src/mission_manager.cpp` | 管理比赛阶段、二维码方向和阶段切换 |
| `motion_controller` | `src/motion_controller.cpp` | 融合巡线、目标检测、黄区边界、雷达、深度和里程计信息，发布底盘速度 |
| `report_manager` | `src/report_manager.cpp` | 接收人物识别结果并生成播报文本 |

默认比赛流程为：

```text
等待发车
  → 普通巡线并避障
  → 接近并识别二维码
  → 按二维码方向执行进黄区动作
  → 搜索黄色通道
  → 黄色通道巡线与避障
  → 判断完成一圈并寻找出口
  → 离开黄区
  → 回程巡线
  → 返回停车区并停车
```

控制系统不是 Nav2/TEB 路径规划器。普通障碍和黄色区域障碍主要通过视觉检测结果及规则控制进行避让；激光雷达和深度图用于近距离安全兜底，是否启用由参数决定。

## 3. `fangan_core` 源码结构

```text
fangan_core/
├── CMakeLists.txt
├── package.xml
├── config/
│   └── race_params.yaml
├── include/fangan_core/
│   ├── logic.hpp
│   ├── mission_manager.hpp
│   ├── motion_controller.hpp
│   ├── race_context.hpp
│   ├── race_types.hpp
│   └── report_manager.hpp
├── launch/
│   └── race_bringup.launch.py
├── scripts/
│   ├── qr_voice_node.py
│   └── qr_voice_test.py
├── src/
│   ├── race_main.cpp
│   ├── mission_manager.cpp
│   ├── motion_controller.cpp
│   ├── report_manager.cpp
│   ├── yellow_boundary_node.cpp
│   └── yellow_ring_main.cpp
└── test/
    └── logic_test.cpp
```

### 3.1 当前参与编译的源码

当前 `CMakeLists.txt` 只将以下四个文件编译为 `race_main`：

- `src/race_main.cpp`
- `src/mission_manager.cpp`
- `src/motion_controller.cpp`
- `src/report_manager.cpp`

其中：

- `race_main.cpp` 初始化 ROS 2，建立 `MultiThreadedExecutor`，并加载三个核心节点；
- `mission_manager.cpp` 保存比赛阶段及二维码方向，负责状态机切换；
- `motion_controller.cpp` 是主要运动控制文件，以 50 ms 周期计算并发布 `/cmd_vel`；
- `report_manager.cpp` 将 `/human/result` 转换为 `/race/report_text` 和 `/human/report_done`。

### 3.2 当前未参与编译的源码

- `src/yellow_boundary_node.cpp` 已复制进 `fangan_core/src`，但当前未加入 `fangan_core/CMakeLists.txt`，不会由 `fangan_core` 编译。
- `src/yellow_ring_main.cpp` 当前未加入 `CMakeLists.txt`，不会生成可执行程序。
- 包根目录下的 `mission_manager.cpp` 不是当前编译入口；实际编译的是 `src/mission_manager.cpp`。

黄色通道视觉节点仍按独立 ROS 2 包运行。启动文件调用的是：

```text
package: yellow_boundary
executable: yellow_boundary_node
```

因此，完整工作区必须保留一个可编译的独立 `yellow_boundary` 包。不能因为 `fangan_core/src` 中存在同名源码，就删除独立包。

## 4. 状态机说明

程序定义了以下比赛阶段：

| 阶段 | 含义 |
| --- | --- |
| `WAIT_START` | 节点已启动，等待发车指令 |
| `LINE_TO_QR` | 普通巡线、避障并接近二维码 |
| `QR_DETECT` | 等待或解析二维码方向 |
| `QR_HARDCODE_TURN_1` | 二维码后第一段固定转向 |
| `QR_ENTRY_ADVANCE` | 向通道入口前进 |
| `QR_HARDCODE_TURN_2` | 第二段固定转向 |
| `QR_HARDCODE_ENTRY` | 兼容旧版固定进通道动作 |
| `BACK_FROM_QR` | 兼容旧版二维码后退阶段 |
| `TURN_TO_GATE` | 对准黄色通道入口 |
| `FORWARD_TO_GATE` | 向入口前进 |
| `YELLOW_SEARCH` | 搜索并锁定黄色通道 |
| `YELLOW_LAP` | 黄色通道巡线、避障和一圈判断 |
| `EXIT_GATE` | 对准黄区出口 |
| `FORWARD_EXIT_GATE` | 驶离黄色区域并寻找普通巡线 |
| `LINE_BACK_HOME` | 回程巡线和避障 |
| `STOP` | 完成任务并停车 |

通过以下命令可实时查看状态和方向：

```bash
ros2 topic echo /race/stage
ros2 topic echo /race/direction
```

二维码方向由 `/qr_code_result` 的文本解析，常见内容为 `clockwise`、`cw`、`counterclockwise`、`anticlockwise` 或 `ccw`。

## 5. 完整提交材料目录

推荐将完整工程整理为以下结构：

```text
RoboCar/
├── src/
│   └── vendor/
│       ├── fangan_core/
│       ├── detector_line/          # ROS 包名 line，包含 r18.bin
│       ├── detector_human/         # ROS 包名 yolo，包含 81.bin
│       ├── detector_qr/model/      # 二维码检测模型
│       ├── origincar_base/
│       └── yellow_boundary/        # 独立 ROS 2 包
├── scripts/
│   ├── qr_to_yellow_hardcode.py
│   ├── start_all.sh
│   ├── go.sh
│   └── stop_all.sh
├── build/                          # 可不提交，可重新生成
├── install/                        # 可不提交，可重新生成
└── log/                            # 不需要提交
```

完整运行环境还必须能够找到以下外部 ROS 包：

- `qr_reader`
- `deptrum-ros-driver-aurora930`
- `usb_cam`
- `ai_msgs`
- `dnn_node`
- `hobot_cv`
- `hbm_img_msgs`
- `sunrise_msgs`
- `origincar_msg`
- `origincar_description`
- `serial`
- `robot_localization`
- `ackermann_msgs`
- 可选：`lslidar_driver`、`hobot_codec`、`websocket`

> 当前备份中缺少工作区顶层的 `scripts/qr_to_yellow_hardcode.py`。虽然启动参数 `start_qr_hardcode` 默认是 `false`，但 `fangan_core/CMakeLists.txt` 会在编译安装阶段读取该文件，因此缺失时仍会导致构建失败。正式提交前必须补齐该脚本，或由维护者调整构建配置后重新验证；不能直接忽略。

### 5.1 必须提交的模型

至少检查以下模型存在且文件大小不为 0：

```text
src/vendor/detector_line/models/r18.bin
src/vendor/detector_human/models/81.bin
src/vendor/detector_qr/model/detect.caffemodel
src/vendor/detector_qr/model/detect.prototxt
src/vendor/detector_qr/model/sr.caffemodel
src/vendor/detector_qr/model/sr.prototxt
```

## 6. 硬件与系统环境

### 6.1 已验证的小车环境

- 操作系统：Ubuntu 22.04，ARM64/aarch64；
- ROS：ROS 2 Humble 或地平线 TROS Humble；
- 底盘：RoboCar 原车底盘和串口控制板；
- 普通视觉：USB 单目相机，默认设备 `/dev/video0`；
- 深度视觉：Deeprum Aurora 930，使用 RGB 和深度图；
- 巡线模型：`line` 包中的 `r18.bin`；
- 障碍物模型：`yolo` 包中的 `81.bin`；
- 可选安全传感器：N10 激光雷达；
- 语音设备：USB 声卡或扬声器，默认配置为 `plughw:2,0`。

### 6.2 基础环境加载

每个新终端先执行：

```bash
if [ -f /opt/tros/humble/setup.bash ]; then
  source /opt/tros/humble/setup.bash
else
  source /opt/ros/humble/setup.bash
fi

source /userdata/skyline/install/setup.bash 2>/dev/null || true
```

`/userdata/skyline` 通常提供地平线板端视觉依赖。实际路径以小车镜像为准。

### 6.3 系统及 Python 依赖

在小车镜像已经预装 TROS 和硬件驱动的前提下，可检查或安装通用工具：

```bash
sudo apt update
sudo apt install -y \
  python3-colcon-common-extensions \
  python3-rosdep \
  libopencv-dev \
  alsa-utils \
  espeak-ng \
  mpg123

python3 -m pip install --user edge-tts
```

不要用通用 Ubuntu 软件源覆盖板端已经安装的 `dnn_node`、`hobot_cv`、Aurora 驱动或地平线运行库。此类依赖应使用小车原有系统镜像或厂家提供的安装包。

## 7. 编译前检查

以下命令均在小车完整工作区 `/userdata/RoboCar` 执行。

### 7.1 检查关键源码和脚本

```bash
cd /userdata/RoboCar

test -f src/vendor/fangan_core/CMakeLists.txt
test -f src/vendor/fangan_core/config/race_params.yaml
test -f src/vendor/fangan_core/launch/race_bringup.launch.py
test -f scripts/qr_to_yellow_hardcode.py
test -f src/vendor/detector_line/models/r18.bin
test -f src/vendor/detector_human/models/81.bin
```

若其中任意命令没有返回成功，先补齐文件再编译。

### 7.2 检查 ROS 包

```bash
source /opt/tros/humble/setup.bash
source /userdata/skyline/install/setup.bash 2>/dev/null || true
source install/setup.bash 2>/dev/null || true

for package in \
  fangan_core origincar_base line yolo yellow_boundary qr_reader \
  deptrum-ros-driver-aurora930 ai_msgs; do
  ros2 pkg prefix "$package" >/dev/null 2>&1 \
    && echo "OK   $package" \
    || echo "MISS $package"
done
```

首次编译前，本工作区中的源码包可能显示 `MISS`，这是正常的；外部驱动和消息包不能缺失。

### 7.3 检查设备

```bash
ls -l /dev/video* 2>/dev/null
ls -l /dev/ttyUSB* /dev/ttyACM* 2>/dev/null
aplay -l
```

如使用 N10 雷达，还需检查对应串口或网络接口。确认串口没有被旧进程占用后再启动。

## 8. 编译方法

### 8.1 完整工作区编译

```bash
cd /userdata/RoboCar

if [ -f /opt/tros/humble/setup.bash ]; then
  source /opt/tros/humble/setup.bash
else
  source /opt/ros/humble/setup.bash
fi

source /userdata/skyline/install/setup.bash 2>/dev/null || true

colcon build \
  --merge-install \
  --parallel-workers 1 \
  --cmake-args -DCMAKE_BUILD_TYPE=RelWithDebInfo
```

小车内存有限，建议使用 `--parallel-workers 1`，避免并行编译时 `cc1plus` 被系统杀死。

### 8.2 只重新编译核心包

修改 `fangan_core` 后可执行：

```bash
cd /userdata/RoboCar
source /opt/tros/humble/setup.bash 2>/dev/null || source /opt/ros/humble/setup.bash
source /userdata/skyline/install/setup.bash 2>/dev/null || true
source install/setup.bash 2>/dev/null || true

MAKEFLAGS=-j1 colcon build \
  --merge-install \
  --packages-select fangan_core \
  --parallel-workers 1 \
  --cmake-args -DCMAKE_BUILD_TYPE=RelWithDebInfo
```

如果独立 `yellow_boundary` 包尚未编译，先执行：

```bash
colcon build \
  --merge-install \
  --packages-select yellow_boundary \
  --parallel-workers 1 \
  --cmake-args -DCMAKE_BUILD_TYPE=RelWithDebInfo
```

### 8.3 编译结果检查

```bash
source /userdata/RoboCar/install/setup.bash

ros2 pkg prefix fangan_core
ros2 pkg executables fangan_core
ros2 pkg executables yellow_boundary
```

正常情况下，至少应看到：

```text
fangan_core race_main
fangan_core qr_voice_node.py
yellow_boundary yellow_boundary_node
```

如果材料保留了顶层脚本安装规则，还应看到：

```text
fangan_core qr_to_yellow_hardcode.py
```

## 9. 启动和操作

### 9.1 安全准备

首次运行或参数变更后测试时：

1. 将驱动轮架空，或把小车放在宽阔、无人员的区域；
2. 确认电池电量、急停方式和底盘串口；
3. 确认没有旧的比赛节点仍在发布 `/cmd_vel`；
4. 准备一个终端专门执行停车命令；
5. 节点启动后先不要发送 `/race/start`。

检查 `/cmd_vel` 发布者：

```bash
ros2 topic info /cmd_vel --verbose
```

比赛控制期间不应有多个互相竞争的运动控制节点直接发布非零 `/cmd_vel`。

### 9.2 一键启动

完整提交工程包含脚本时，推荐使用：

```bash
cd /userdata/RoboCar
bash scripts/start_all.sh
```

该脚本应先停止旧进程，再启动感知、底盘和 `fangan_core`。启动成功只表示节点开始运行，小车仍应处于 `WAIT_START`。

### 9.3 直接使用 ROS 2 启动

没有一键脚本时可执行：

```bash
cd /userdata/RoboCar
source /opt/tros/humble/setup.bash 2>/dev/null || source /opt/ros/humble/setup.bash
source /userdata/skyline/install/setup.bash 2>/dev/null || true
source install/setup.bash

ros2 launch fangan_core race_bringup.launch.py start_lidar:=false
```

常用启动参数：

| 参数 | 默认值 | 作用 |
| --- | --- | --- |
| `start_camera_stack` | `true` | 启动 USB 相机链路 |
| `camera_device` | `/dev/video0` | USB 单目相机设备 |
| `start_depth_camera` | `true` | 启动 Aurora 930 RGB/深度驱动 |
| `start_base` | `true` | 启动原车底盘串口节点 |
| `start_line` | `true` | 启动巡线模型 |
| `start_yolo` | `true` | 启动目标检测模型 |
| `start_qr_stack` | `true` | 启动二维码识别节点 |
| `start_qr_voice` | `true` | 启动语音播报节点 |
| `start_qr_hardcode` | `false` | 启动旧版 Python 固定动作节点 |
| `start_yellow_boundary` | `true` | 启动黄色/绿色边界检测 |
| `start_lidar` | `false` | 启动 N10 雷达驱动 |
| `start_web_preview` | `false` | 启动网页图像预览 |
| `params_file` | 包内 `config/race_params.yaml` | 指定比赛参数文件 |

例如，禁用语音并开启雷达：

```bash
ros2 launch fangan_core race_bringup.launch.py \
  start_qr_voice:=false \
  start_lidar:=true
```

### 9.4 发车

确认传感器、话题和状态正常后，再在另一个终端执行：

```bash
cd /userdata/RoboCar
source /opt/tros/humble/setup.bash 2>/dev/null || source /opt/ros/humble/setup.bash
source install/setup.bash
bash scripts/go.sh
```

也可以直接发布一次启动消息：

```bash
ros2 topic pub --once /race/start std_msgs/msg/Bool "{data: true}"
```

发车后状态应从 `WAIT_START` 切换为 `LINE_TO_QR`。

### 9.5 安全停车

推荐执行完整停车脚本：

```bash
cd /userdata/RoboCar
bash scripts/stop_all.sh
```

如果在前台直接运行 `ros2 launch`，按 `Ctrl+C` 会停止启动文件管理的节点。随后仍应确认底盘已收到零速度，并检查是否存在残留发布者：

```bash
ros2 topic info /cmd_vel --verbose
```

紧急情况下优先使用小车物理急停或断开驱动电源。不要把关闭 SSH 窗口当成可靠停车方法，因为后台节点可能继续运行。

## 10. 主要节点、话题和数据流

### 10.1 感知输入

| 话题 | 类型 | 来源/作用 |
| --- | --- | --- |
| `/image` | `sensor_msgs/msg/Image` | USB 单目相机图像，供普通巡线和黄色边界检测使用 |
| `/aurora/rgb/image_raw` | `sensor_msgs/msg/Image` | Aurora RGB 图像；当前二维码节点通过重映射接到 `/image` |
| `/aurora/depth/image_raw` | `sensor_msgs/msg/Image` | 深度图，用于近距离安全判断 |
| `/racing_track_center_point` | `geometry_msgs/msg/PointStamped` | 普通巡线中心点 |
| `/race/detections` | `std_msgs/msg/String` | YOLO 目标检测结果 |
| `/qr_code_result` | `std_msgs/msg/String` | 二维码解码文本 |
| `/yellow_boundary_center` | `geometry_msgs/msg/PointStamped` | 黄色区域中心 |
| `/green_boundary_center` | `geometry_msgs/msg/PointStamped` | 绿色禁入区域中心 |
| `/yellow_lane_info` | `std_msgs/msg/Float64MultiArray` | 黄色通道左右边界、绿色和格子区域信息 |
| `/scan` | `sensor_msgs/msg/LaserScan` | N10 激光雷达数据 |
| `/odom` | `nav_msgs/msg/Odometry` | 底盘里程计 |
| `/imu` | `sensor_msgs/msg/Imu` | 姿态数据 |

### 10.2 控制和状态输出

| 话题 | 类型 | 作用 |
| --- | --- | --- |
| `/cmd_vel` | `geometry_msgs/msg/Twist` | 最终底盘速度指令 |
| `/race/start` | `std_msgs/msg/Bool` | 比赛发车信号 |
| `/race/stage` | `std_msgs/msg/String` | 当前状态机阶段 |
| `/race/direction` | `std_msgs/msg/String` | 顺时针或逆时针方向 |
| `/race/qr_reached` | `std_msgs/msg/Bool` | 已到达二维码位置 |
| `/race/yellow_entry_reached` | `std_msgs/msg/Bool` | 已到黄色通道入口 |
| `/race/loop_complete` | `std_msgs/msg/Bool` | 黄色通道一圈完成 |
| `/race/line_reacquired` | `std_msgs/msg/Bool` | 出黄区后重新找到普通巡线 |
| `/race/return_complete` | `std_msgs/msg/Bool` | 回程完成 |
| `/race/report_text` | `std_msgs/msg/String` | 待播报的任务/识别文本 |

### 10.3 语音播报

`qr_voice_node.py` 订阅：

- `/qr_code_result`
- `/race/report_text`
- `/llm`

默认优先使用 `edge-tts`，失败后回退到 `espeak-ng`。启动文件默认音频设备为 `plughw:2,0`。

检查声卡：

```bash
aplay -l
```

单独测试播报话题：

```bash
ros2 topic pub --once /llm std_msgs/msg/String "{data: '语音测试正常'}"
```

若没有声音，应根据 `aplay -l` 的结果修改启动文件中的 `audio_device`，并检查 `edge-tts`、`mpg123`、`espeak-ng` 和网络状态。

## 11. 参数配置

### 11.1 实际生效的参数文件

默认启动文件加载的是：

```text
fangan_core/config/race_params.yaml
```

包根目录还存在另一个：

```text
fangan_core/race_params.yaml
```

根目录这份文件不会被默认 `race_bringup.launch.py` 加载。比赛调参应修改 `config/race_params.yaml`，或在启动时通过 `params_file:=...` 明确指定其他文件。

### 11.2 当前关键参数

| 参数 | 当前值 | 说明 |
| --- | ---: | --- |
| `follow_speed` | `0.672` | 去程普通巡线速度 |
| `back_line_speed` | `0.36` | 回程巡线速度 |
| `line_kp` | `0.0035` | 普通巡线转向比例系数 |
| `line_lost_speed` | `0.0` | 丢线时线速度；当前设为停车 |
| `qr_stop_area` | `9500.0` | 二维码接近停止的视觉面积阈值 |
| `qr_visual_lock_ticks_required` | `5` | 二维码目标连续确认次数 |
| `yellow_lane_speed` | `0.264` | 黄色通道巡线速度 |
| `yellow_lane_kp` | `0.003` | 黄色通道转向比例系数 |
| `yellow_loop_green_kp` | `0.0015` | 绿色内区修正系数 |
| `yellow_loop_yellow_kp` | `0.0017` | 黄色边界修正系数 |
| `yellow_loop_green_weight` | `0.80` | 黄区绿色信息融合权重 |
| `obstacle_trigger_area` | `8000.0` | 普通区域视觉障碍触发面积 |
| `yellow_obstacle_trigger_area` | `7000.0` | 黄区视觉障碍触发面积 |
| `use_lidar_safety` | `false` | 是否启用雷达近距安全层 |
| `use_depth_safety` | `false` | 是否启用深度近距安全层 |

修改速度、转向、避障或进通道动作后，需要重新启动节点；如果只是修改安装空间中的 YAML 之外的源码，则必须重新编译。

> 当前深度安全默认关闭。源码注释说明 Aurora 930 在当前安装角度和分辨率下可能把地面散斑当作近距障碍。启用前应先架空车辆，在空旷区域检查深度 ROI，不要在正式比赛时直接打开未验证的深度制动。

## 12. 启动后的验收检查

### 12.1 节点检查

```bash
ros2 node list | sort
```

应能够看到底盘、相机、巡线、目标检测、二维码、黄色边界以及 `fangan_core` 的相关节点。

### 12.2 话题检查

```bash
for topic in \
  /cmd_vel \
  /race/start \
  /race/stage \
  /qr_code_result \
  /racing_track_center_point \
  /race/detections \
  /yellow_boundary_center \
  /yellow_lane_info; do
  ros2 topic list | grep -qx "$topic" \
    && echo "OK   $topic" \
    || echo "MISS $topic"
done
```

### 12.3 数据频率检查

```bash
ros2 topic hz /image
ros2 topic hz /racing_track_center_point
ros2 topic hz /race/detections
ros2 topic hz /yellow_lane_info
```

如果启用了雷达或深度安全，再检查：

```bash
ros2 topic hz /scan
ros2 topic hz /aurora/depth/image_raw
```

### 12.4 静止发车检查

小车架空后发送发车信号，观察：

```bash
ros2 topic echo /race/stage
ros2 topic echo /cmd_vel
```

确认状态从 `WAIT_START` 进入 `LINE_TO_QR`，且转向方向与画面中的巡线偏差一致。检查完成后执行 `scripts/stop_all.sh`。

## 13. 常见问题

### 13.1 编译时报 `qr_to_yellow_hardcode.py` 不存在

原因：`CMakeLists.txt` 中存在相对路径：

```text
../../../scripts/qr_to_yellow_hardcode.py
```

处理：确认完整工作区顶层存在 `scripts/qr_to_yellow_hardcode.py`。当前 `002605` vendor 备份本身不包含该文件。

### 13.2 启动时报 `PackageNotFoundError`

先确认包是否可见：

```bash
ros2 pkg prefix 包名
```

若源码已经存在但仍找不到，重新编译并执行：

```bash
source /userdata/RoboCar/install/setup.bash
```

若缺少 `qr_reader`、Aurora 驱动或地平线依赖，应恢复小车原有安装环境，不要只复制 `vendor` 目录。

### 13.3 `yellow_boundary` 找不到

`src/yellow_boundary_node.cpp` 不会自动产生 `yellow_boundary_node`。必须保留并编译独立包：

```text
src/vendor/yellow_boundary/
├── CMakeLists.txt
├── package.xml
└── src/yellow_boundary_node.cpp
```

然后执行：

```bash
colcon build --merge-install --packages-select yellow_boundary
source install/setup.bash
```

### 13.4 相机没有 `/image`

检查设备和节点：

```bash
ls -l /dev/video*
ros2 node list
ros2 topic info /image --verbose
```

如果系统没有安装 `usb_cam`，当前启动文件会打印提示并跳过 USB 相机节点，此时依赖 `/image` 的普通巡线和黄色边界检测均无法正常工作。

### 13.5 二维码无法识别

检查：

```bash
ros2 topic hz /image
ros2 topic echo /qr_code_result
test -d /userdata/RoboCar/src/vendor/detector_qr/model
```

当前启动文件的 `qr_model_dir` 默认值是 `/userdata/fangan/src/vendor/detector_qr/model`，与推荐工作区 `/userdata/RoboCar` 不一致。使用 RoboCar 工作区时，应在启动命令中明确覆盖：

```bash
ros2 launch fangan_core race_bringup.launch.py \
  qr_model_dir:=/userdata/RoboCar/src/vendor/detector_qr/model
```

也可在正式提交前统一启动文件中的默认路径，并重新验证。

### 13.6 小车启动后立即运动

正常情况下，节点启动后处于 `WAIT_START`，收到 `/race/start` 才进入运行阶段。如果未发车就运动：

1. 立即执行 `bash scripts/stop_all.sh`；
2. 检查 `/cmd_vel` 是否存在多个发布者；
3. 检查是否有旧版控制程序、测试节点或后台进程；
4. 架空车辆后再重新启动。

### 13.7 编译被系统杀死

如果日志出现 `cc1plus: fatal error: Killed`，通常是内存不足。使用：

```bash
MAKEFLAGS=-j1 colcon build --merge-install \
  --packages-select fangan_core \
  --parallel-workers 1
```

同时停止未使用的感知和比赛进程。

### 13.8 没有语音

```bash
aplay -l
which edge-tts
which mpg123
which espeak-ng
```

确认启动文件中的 `audio_device` 与实际声卡编号相同。`edge-tts` 需要网络；无法联网时会回退到 `espeak-ng`。

## 14. 提交材料清单

正式提交前逐项检查：

- [ ] 完整 ROS 2 工作区，而不是只有 `vendor` 的备份；
- [ ] `fangan_core`、`line`、`yolo`、`origincar_base`、`yellow_boundary` 源码包；
- [ ] 二维码模型和 `qr_reader` 的来源/安装说明；
- [ ] 巡线模型 `r18.bin` 和障碍物模型 `81.bin`；
- [ ] `scripts/qr_to_yellow_hardcode.py`；
- [ ] 一键启动、发车和停车脚本；
- [ ] `config/race_params.yaml`；
- [ ] 本使用说明；
- [ ] 依赖版本或小车系统镜像说明；
- [ ] 编译日志，末尾无 `Failed` 包；
- [ ] 启动日志，关键节点和话题均存在；
- [ ] 架空轮测试及物理急停测试记录。

不建议提交 `build/`、`log/`、缓存、临时图像和编辑器数据库。是否提交 `install/` 取决于比赛要求；若要求源码可复现，应以源码和编译说明为准。

## 15. 最终验收流程

建议在正式提交前按以下顺序完整执行一次：

1. 在与比赛小车相同的系统镜像中准备 `/userdata/RoboCar`；
2. 检查源码包、模型、脚本和外部驱动；
3. 删除或移走旧的 `build/`、`install/`、`log/` 后进行一次干净编译；
4. 确认 `colcon build` 汇总中没有失败包；
5. 执行 `scripts/start_all.sh`，但先不发车；
6. 检查节点、话题、图像和感知输出；
7. 架空车辆后发送 `/race/start`；
8. 验证阶段切换和 `/cmd_vel` 输出；
9. 执行 `scripts/stop_all.sh` 并确认车轮停止；
10. 在封闭场地低速完成一次全流程实车测试，保存编译日志和运行日志。

只有实际完成上述编译和实车验收，才能证明提交材料在目标小车环境中可以正常编译和运行。本文档中的命令来源于当前工程结构，但不能替代最终的硬件测试结果。

